[tasks]
names = ["RestingState", "surroundSupp", "contrastChangeDetection"]

[releases]
names = ["R1", "R2", "R3", "R4", "R6", "R7", "R8", "R9", "R10", "R11"]

[training]
epochs = 10
batch_size = 128
num_workers = 8
learning_rate = 1e-3
weight_decay = 1e-5
seed = 42
device = "auto"
use_amp = false
log_every_steps = 25
clip_grad_norm = 0.0
save_every_epochs = 5
output_dir = "weights/masked_ae"
val_fraction = 0.1
dataset_variant = "mini"
time_mask_fraction = 0.15
channel_mask_fraction = 0.25
mask_value = 0.0

[wandb]
enabled = false
project = "eeg2025"
entity = ""
run_name = "masked-autoencoder"
mode = "online"
