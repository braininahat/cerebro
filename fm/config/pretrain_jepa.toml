[tasks]
names = ["RestingState", "surroundSupp", "contrastChangeDetection"]

[releases]
names = ["R1", "R2", "R3", "R4", "R6", "R7", "R8", "R9", "R10", "R11"]

[training]
epochs = 10
batch_size = 128
num_workers = 8
window_size_s = 4.0
stride_s = 2.0
crop_size_s = 2.0
learning_rate = 0.001
weight_decay = 0.0001
seed = 42
device = "auto"
use_amp = true
log_every_steps = 25
clip_grad_norm = 0.0
save_every_epochs = 5
output_dir = "weights/pretrain_jepa"
val_fraction = 0.2
dataset_variant = "mini"

[wandb]
enabled = false
project = "eeg2025"
entity = ""
run_name = "jepa-pretraining"
mode = "online"
